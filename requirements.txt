# Install via pip:
flask>=3.0.0
pillow>=10.0.0
psutil>=5.9.0
playwright>=1.40.0
qrcode[pil]>=7.4.0

# Install via apt on Pi OS Bookworm (NOT pip):
#   sudo apt install python3-picamera2 python3-lgpio python3-gpiozero
#
# After pip install playwright, run:
#   playwright install chromium
